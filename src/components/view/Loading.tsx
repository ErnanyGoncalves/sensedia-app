import { Typography } from "@mui/material"

export const Loading = () => {
    return (
        <Typography sx={{ fontSize: 16 }} color="text.primary">
            Loading...
        </Typography>
    )
}